<ul class="list-group">
    {% for location in locations %}
        <li class="list-group-item"><a href="{{ path('location_show', { locationId : location.id }) }}">{{ location.name }}</a></li>
    {% endfor %}
</ul>